// Generated by scripts/render-calculator-help.ts with Bun.markdown.
export const HELP_CONTENT_HTML = "<ul>\n<li>\n<p><strong>Natural input</strong>\nImplicit multiplication works: <code>2x</code>, <code>2(x+1)</code>, and <code>(x+1)(x-1)</code>. Functions accept ordinary parentheses; curly braces are optional.</p>\n</li>\n<li>\n<p><strong>Keyboard shortcuts</strong>\nCtrl+C opens Calculator, Ctrl+P opens Plot, Ctrl+R focuses Relations, and Ctrl+L opens Library. Ctrl+C copies normally when text is selected. Escape toggles focus between the expression field and the operation tabs. Outside the expression field, press a group key followed by an operation key: R then P adds, R then S subtracts. Group keys are R (Arithmetic), S (Scientific), A (Algebra), C (Calculus), and P (Probability). The tab shows the group key; each operation shows only its second key. Inserting an operation returns focus to the expression. Escape also cancels a pending sequence and returns to the expression. Letter shortcuts are disabled while typing formulas or using a dialog.</p>\n</li>\n<li>\n<p><strong>Shared system</strong>\nSave equations as constraints and bare expressions as queries. Calculate uses every saved row together and returns all discrete real solutions.</p>\n</li>\n<li>\n<p><strong>Symbolic work</strong>\nTry <code>factor(x^2-1)</code>, <code>diff(x^3, x)</code>, or <code>integrate(sin(x), x)</code>. Underdetermined systems are reported directly rather than shown with generated parameters.</p>\n</li>\n<li>\n<p><strong>V1 result states</strong>\nNo solution means the real set is proven empty. Unresolved means the request is valid but SymPy could not produce a decisive finite result. Unsupported identifies a feature outside V1.</p>\n</li>\n<li>\n<p><strong>Probability distributions</strong>\nThe Probability tab inserts distribution and query functions at the cursor, or wraps selected text. Define random variables in Relations: <code>X = Norm(0, 1)</code>, <code>Y = Pois(4)</code>, or <code>W = Weib(2, 5)</code>. Use <code>P(X &lt; 3.1)</code>, <code>P(-1 &lt;= X &lt;= 1)</code>, <code>P(Y = 3)</code>, or inline <code>P(3.1 &gt; N(0, 1))</code>. Discrete <code>&lt;</code> and <code>&lt;=</code> have different meanings. <code>E(X)</code>, <code>Var(X)</code> and <code>quantile(X, 0.95)</code> calculate the mean, variance and percentile cutoff.</p>\n</li>\n<li>\n<p><strong>Distribution parameters</strong>\n<code>Norm(mean, variance)</code> (alias <code>N</code>), <code>Pois(rate)</code>, <code>Bin(trials, probability)</code>, <code>Bern(probability)</code>, <code>Unif(lower, upper)</code>, <code>Exp(rate)</code>, <code>Weib(shape, scale)</code>, <code>Gamma(shape, scale)</code>, <code>Beta(alpha, beta)</code>, <code>T(degrees)</code>, <code>Chi2(degrees)</code>, and <code>F(degrees1, degrees2)</code>. Names are case-sensitive: <code>Exp</code> is a distribution; <code>exp</code> is the exponential function. Use <code>Weib</code>, not <code>Wieb</code>.</p>\n</li>\n<li>\n<p><strong>Probability plots</strong>\nTick distribution declarations and open Plot to see density curves or discrete probability bars. Tick a <code>P(...)</code> query to highlight its event. Keep the declarations and numeric parameter definitions it uses ticked. The plot frames the distributions automatically; Reset view restores that range. Continuous densities and discrete masses use separate plots. Numerical results and plots are approximate.</p>\n</li>\n<li>\n<p><strong>Probability scope</strong>\nParameters and cutoffs must be numeric or supplied by direct numeric definitions such as <code>mu = 3</code>. Affine transformations such as <code>Y = 2*X + 3</code> preserve identity: <code>X-X</code> is zero. Calculations combining different random variables, general nonlinear transformations, and solving probability equations for unknown parameters are not supported yet. Use <code>quantile</code> for inverse cumulative probabilities. Undefined statistics report an error; infinite values may appear in standalone queries.</p>\n</li>\n<li>\n<p><strong>Inline function help</strong>\nType <code>Nor</code>, <code>Wei</code>, <code>dif</code>, or another function prefix to see a grey completion. Press Tab or Right Arrow to insert the function name and opening parenthesis; enter your own arguments using the signature beneath the input. Escape dismisses the suggestion. Calculus hints cover <code>diff(expression, variable)</code>, <code>integrate(expression, variable)</code>, and <code>limit(expression, variable, point)</code>.</p>\n</li>\n</ul>\n<h2>Vectors and matrices</h2>\n<p>Write column vectors as <code>u = [1,2,3]</code> and matrix rows as <code>A = [[1,2],[3,4]]</code>.\nEntries may be exact numbers or symbolic expressions. All rows must have equal length.\nNamed values can refer to other enabled definitions, regardless of row order.</p>\n<ul>\n<li>Add and subtract matching shapes: <code>u+v</code>, <code>A-B</code>; scale with <code>3*u</code> or <code>A/2</code>.</li>\n<li>Multiply matrices with <code>A*B</code> and matrix–vector products with <code>A*u</code>.</li>\n<li>Use <code>dot(u,v)</code> for a dot product and <code>cross(u,v)</code> for a 3D cross product.</li>\n<li><code>norm(u)</code> gives Euclidean length; <code>unit(u)</code> gives a unit vector.</li>\n<li><code>transpose(A)</code>, <code>det(A)</code>, <code>inv(A)</code>, <code>trace(A)</code>, <code>rank(A)</code> provide linear algebra operations.</li>\n<li><code>A^2</code> is a matrix power; powers require square matrices and integer exponents.</li>\n<li><code>linsolve(A,b)</code> solves <code>A*x=b</code>, including rectangular systems. Underdetermined systems return free parameters such as <code>tau0</code>; inconsistent systems produce an error.</li>\n</ul>\n<p><code>diff</code>, <code>integrate</code>, and <code>limit</code> act on every component. For example,\n<code>r=[cos(t),sin(t),t]</code> and <code>diff(r,t)</code> give <code>[-sin(t),cos(t),1]</code>.</p>\n<p>Multivariable calculus uses an explicit ordered list of distinct variables:</p>\n<ul>\n<li><code>grad(x^2+y^2,[x,y])</code>: gradient of a scalar expression.</li>\n<li><code>jacobian([x*y,sin(x)],[x,y])</code>: rows are output components, columns are variables.</li>\n<li><code>div([x^2,y^2],[x,y])</code>: divergence.</li>\n<li><code>curl([y,z,x],[x,y,z])</code>: 3D curl.</li>\n<li><code>hessian(x^2+x*y,[x,y])</code>: matrix of second partial derivatives.</li>\n</ul>\n<p>Grey autocomplete and argument hints support all these functions. Press Tab or Right Arrow to accept.\nResults retain exact fractions and symbolic expressions. Calculus at a defined scalar parameter differentiates before substituting its value.\nIn a workspace containing matrices, equations must be direct <code>name=expression</code> definitions; use <code>linsolve(A,b)</code> for implicit linear systems. Names retain the calculator's case-insensitive behavior, so <code>A</code> and <code>a</code> refer to the same value.</p>\n<h3>Vector plots</h3>\n<p>Select 2D vectors such as <code>[3,4]</code> to plot arrows from the origin, or a two-component expression such as <code>[cos(t),sin(t)]</code> to plot a parametric curve. Curves use a fixed parameter range of −10 to 10, displayed in the legend; pan and zoom change the viewing window. Selected scalar definitions supply parameter values. Vector addition and scalar multiplication can be plotted directly. For other operations, plot the evaluated two-component expression. Matrices, 3D vectors, and vector fields are not plotted in this release.</p>\n"
